spring:
  application:
    name: service-registry
  cloud:
    config:
      uri: ${CONFIG_HOST}
      fail-fast: true

eureka:
  instance:
    prefer-ip-address: true
  client:
    register-with-eureka: false
    fetch-registry: false